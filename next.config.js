/** @type {import('next').NextConfig} */
const nextConfig = {
  images: {
    domains: ['localhost'],
    remotePatterns: [
      {
        protocol: 'https',
        hostname: '**',
      },
    ],
  },
  async rewrites() {
    return [
      {
        source: '/campaign/:slug',
        destination: '/campaign/[slug]',
      },
    ];
  },
};

module.exports = nextConfig;